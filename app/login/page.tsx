import { LoginForm } from "./client"

export default function LoginPage() {
  return (
    <main style={{ display: "grid", minHeight: "100dvh", placeItems: "center" }}>
      <LoginForm />
    </main>
  )
}
